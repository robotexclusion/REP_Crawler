# REP_Crawler
Web crawler to examine REP implementation in the top 1m web domains.

This project utilizes the TRANCO list to identify the top web domains, and then queries them for Robots Exclusion Protocol (REP) implemtations aligning with RFC 9309 , as well as \<meta\> tags in .html text defined by the Web Robots Pages.
